SELECT S.SONG_ID, S.SINGER, S.TITTLE, S.ALBUM, S.REALISE_DATE FROM (SELECT SONG_ID FROM PLAYLIST_SONG WHERE PLAYLIST_ID = :PLAYLIST_ID)
    AS PS RIGHT JOIN SONG AS S ON S.SONG_ID = PS.SONG_ID WHERE PS.SONG_ID IS NULL ORDER BY S.SONG_ID